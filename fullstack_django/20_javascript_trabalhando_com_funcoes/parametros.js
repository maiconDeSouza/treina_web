//parametros são informados na declaração da função
function soma(a, b){ //a e b são parametros
    return a + b
}

//Argumentos são quando são passados os valores na chamada/execução da função
const result = soma(5, 3) // 5, 3 argumentos
console.log(result)